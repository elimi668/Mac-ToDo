name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build PyInstaller exe
        run: |
          pyinstaller --onefile --windowed --name TodoMate --icon=app/resources/icons/app_icon.ico --add-data "app;app" --add-data "alembic;alembic" --add-data "alembic.ini;." --collect-all PySide6 app/main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: TodoMate-windows
          path: dist/TodoMate.exe
